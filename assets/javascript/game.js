$(document).ready(function() {
    // VARIABLES
    // ==================================================================================
    let wins = 0;
    let losses = 0;



    // FUNCTIONS
    // ==================================================================================
    

    $(`#wins`).html(wins);
    $(`#losses`).html(losses);




    $(`#ruby-click`).on(`click`, function() {
        console.log(`You clicked the ruby!`);
    })















});